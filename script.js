const menuBtn=document.getElementById("menuBtn");const navMenu=document.getElementById("navMenu");menuBtn.addEventListener("click",()=>navMenu.classList.toggle("active"));document.querySelectorAll("#navMenu a").forEach(link=>link.addEventListener("click",()=>navMenu.classList.remove("active")));document.getElementById("year").textContent=new Date().getFullYear();console.log("⚡ STUNNER MD website loaded successfully.");document.querySelectorAll(".command").forEach(command=>{command.addEventListener("click",()=>{const commandName=command.querySelector("code").textContent;navigator.clipboard?.writeText(commandName);command.style.borderColor="#00ff88";setTimeout(()=>command.style.borderColor="#222",800)})});
